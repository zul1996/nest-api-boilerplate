export interface DialogMetadata {
    title: String;
    action: String;
    supportMaximized: Boolean;
    fullWidth: Boolean;
    fullHeight: Boolean;
    maximized: Boolean;
    cardStyle: String;
}

